{
  "name": "nexio-financeiro",
  "version": "1.0.0",
  "private": true,
  "description": "Nexio Financeiro Android app wrapper powered by Capacitor.",
  "scripts": {
    "test": "node tests/core.test.js && node tests/design-system.test.js && node tests/security.test.js && node tests/sync.test.js && node tests/accounts.test.js && node tests/budgets.test.js && node tests/notifications.test.js && node tests/ai-assistant.test.js && node tests/speech-recognition.test.js",
    "android:assets:source": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-android-assets.ps1",
    "android:add": "npx cap add android",
    "android:assets": "npx capacitor-assets generate --android",
    "android:overrides": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/apply-capacitor-android-overrides.ps1",
    "android:sync": "npx cap sync android && npm run android:overrides",
    "android:open": "npx cap open android",
    "android:setup": "npm run android:assets:source && npx cap add android && npx capacitor-assets generate --android && npm run android:overrides && npx cap sync android",
    "android:debug": "npx cap run android",
    "android:build:apk": "cd android && gradlew.bat assembleDebug",
    "android:build:aab": "cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/network": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capgo/capacitor-speech-recognition": "^8.1.10"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.0.0"
  }
}
